function plotlogPlot(x, y, x_label, y_label, title_label, position, isNewFig)

loglog(x, y);
xlabel(x_label);
ylabel(y_label);
title(title_label);
movegui(position);

% ============================================================

end
